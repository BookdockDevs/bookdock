{
  "name": "bookdock",
  "private": true,
  "version": "0.3.5",
  "scripts": {
    "dev": "node scripts/sync-version.mjs && node scripts/dev.mjs",
    "build": "node scripts/sync-version.mjs && pnpm --recursive run build",
    "test": "pnpm --recursive --filter @bookdock/server --filter @bookdock/web run test",
    "test:watch": "vitest",
    "lint": "oxlint --deny-warnings",
    "typecheck": "pnpm --recursive run typecheck",
    "version:sync": "node scripts/sync-version.mjs"
  },
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "oxlint": "^1.74.0",
    "vitest": "^4.1.10"
  }
}
